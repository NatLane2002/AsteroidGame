# Dependencies
node_modules/
package-lock.json
# (Optional: keep package-lock.json if you want lock versions, but usually safe to ignore for simple projects if you want less noise. 
# Standard practice is to KEEP package-lock, but I will ignore node_modules which is the big one.)

# Environment variables
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
Thumbs.db

# Editor directories
.idea/
.vscode/
*.swp
*.swo
